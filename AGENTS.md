# DividendLab Agent Guide

## 1. 项目定位

DividendLab 是一个面向 A 股红利类 ETF 的研究与策略回测 Agent，主要能力包括：

1. 获取并更新红利 ETF 及相关指数数据。
2. 按明确规则筛选和比较红利 ETF。
3. 使用 Python 编写交易策略。
4. 调用回测工具评估交易策略。

项目只提供研究和回测能力，不直接构成投资建议，不承诺收益，也不连接实盘交易。所有结论都必须有可追溯的证据和明确的计算口径。

项目最终以 Web 应用形式向用户提供服务，用户通过 Web UI 与 Agent 交互。系统采用前后端分离架构，但当前开发重点是 Agent 及其领域能力；Agent 稳定后再开发后端服务，最后接入和完善 Web 前端。

## 2. 初期范围

### 包含

- 中国大陆交易所上市的红利、股息、低波红利、央企红利和行业红利 ETF。
- 日级别行情、分红、基金基本信息、跟踪指数及行业权重数据。
- 单只 ETF 和 ETF 组合的研究与回测。
- Python 策略编写、校验和回测。

### 暂不包含

- 分钟级、高频和日内交易。
- 自动下单、券商账户管理和实盘交易。
- 对未来收益、分红或投资结果作保证。
- 没有来源或无法复现的主观推荐。

## 3. 基本原则

### 数据优先

- 研究前检查数据是否更新到目标交易日；数据陈旧时先更新或明确说明。
- 优先使用交易所、基金公司和指数公司的官方数据，第三方数据用于补充和交叉验证。
- 关键数据保留来源、适用日期和更新时间。
- 不使用未来数据。回测只使用 ETF 自身的历史日行情和分红，不读取指数成分与行业权重，因此不存在用当前成分替代历史成分的问题。
- 数据缺失或来源冲突时不得静默补齐，必须在结果中说明。

### 证据链优先

Agent 做出任何判断，都必须有明确、充足、直接相关且可复现的证据支持。证据链至少应能说明：

```text
数据来源 -> 数据时间范围 -> 数据处理 -> 指标计算 -> 筛选/回测规则 -> 结果 -> 结论
```

结果至少要区分：

- **事实**：来自公告、交易所、指数公司、基金文件或可靠数据源。
- **计算结果**：基于固定数据和明确公式计算得到的指标。
- **假设**：基准、交易成本、分红再投资、成交时点等人为设定。
- **判断**：依据事实、计算结果和规则形成的结论。

证据不足、过期、冲突或无法复现时，只能输出“证据不足/待核验”和缺失项，不得输出确定性结论。最终结果必须能查看来源、数据区间、计算口径、使用规则和限制。

## 4. 红利 ETF 筛选规则

筛选分为硬性条件、排序指标和风险说明。具体阈值先保持可配置，后续根据真实数据和使用过程逐步确定。

### 硬性条件

候选 ETF 必须同时满足：

1. 能够确认属于红利相关 ETF，并能确认其跟踪指数。
2. 跟踪指数的行业权重达到“不均匀”的要求。行业权重越不均匀，筛选优先级越高；具体使用 HHI、前一/前三行业权重或行业权重熵衡量。行业权重一律使用最新一期截面数据。
3. 最近连续 12 个完整自然月均有实际分红记录，且记录来自正式公告或可靠分红数据。仅“允许按月分红”不算满足月月分红。
4. 有足够的日级别行情、分红和行业数据进行计算。
5. 基金处于正常交易状态，并满足后续设定的规模和流动性要求。

### 排序指标

- 行业权重不均匀程度。
- 近 12 个月已实现分红收益率和分红稳定性。
- 最大回撤、回撤修复时间和未修复回撤。
- 波动率、下行风险、跟踪误差、规模、成交额和费率。

行业集中是本项目的筛选偏好，但结果必须同时说明集中行业及其风险。硬性条件未满足的 ETF 不进入正式排名，应放入观察或待核验列表，并写明原因。

### 关键计算口径

- 分红收益率必须说明计算区间和价格/净值分母。
- 默认同时保留价格收益和包含现金分红再投资的总收益。
- 最大回撤和修复时间优先基于日频总收益序列计算。
- ETF 比较使用共同可比区间，同时保留各自完整历史区间结果。
- 行业权重不均匀度基于最新一期指数成分权重计算，只反映当期状态，不做历史回溯；结果必须标注权重截面日期。
- 行业权重指标只用于当期筛选，不得作为回测信号或策略输入。

## 5. 数据工具

首期先实现一个数据同步工具：`sync_dividend_etf_data`。

它负责：

1. 发现和维护 A 股红利 ETF 候选池。
2. 获取日级别行情、分红、基金信息、跟踪指数和行业权重。
3. 支持首次全量同步和后续增量更新。
4. 做基本的数据完整性检查。
5. 输出最新数据日期、更新范围、来源和缺失项。

首期最低字段：

| 数据 | 字段示例 |
| --- | --- |
| ETF 基本信息 | 代码、名称、上市日、基金公司、跟踪指数、状态 |
| 日行情 | 交易日、开高低收、成交量、成交额、净值或复权数据 |
| 分红 | 除息日、派息日、每份分红、公告来源 |
| 指数行业 | 指数代码、行业名称、行业权重、权重截面日期（仅最新一期，不建历史序列） |

原始数据与标准化数据应分开保存，但存储技术和数据源数量暂不在本阶段规定。

## 6. 策略与回测工具

### 策略

策略优先使用 Python。策略针对特定 ETF 或候选池编写，可以读取截至回测当日已知的行情和分红数据、候选 ETF、当前持仓和现金，并输出目标权重或交易指令。策略不读取指数成分与行业权重。

新生成或修改的策略，必须先展示规则和参数并通过基本校验，未经用户确认不得执行。

### 回测

回测必须作为 Agent 可以调用的独立工具 `run_backtest`，不能把回测计算写在提示词或 Agent 节点中。回测服务应与 Agent 解耦，能够单独测试。

回测的数据输入仅限 ETF 自身的日行情和分红记录，不涉及指数成分或行业权重。

首期输入至少包括：

- 策略或策略版本。
- ETF 代码或候选池。
- 回测开始日期和结束日期。
- 初始资金。
- 基准。
- 成交、费用、滑点和分红处理方式。

首期输出至少包括：

- 累计收益、年化收益、波动率、最大回撤和回撤修复时间。
- 基准对比、交易次数、换手率、交易成本和分红处理结果。
- 回测区间、数据版本、策略版本、参数和关键假设。
- 组合净值、交易记录和证据引用。

回测必须使用日级别数据并避免前视偏差。策略、参数、数据和关键回测假设必须能够复现同一次结果。Python 策略执行环境需要隔离，具体隔离技术后续确定。

## 7. Agent 架构与 Workflow

### 初步技术选择

采用 **LangGraph** 作为 Agent 的主编排框架，使用显式的 Graph Workflow；可以使用 `langchain-core` 的模型和工具接口，但不把 LangChain 的自由循环 Agent 作为主要业务流程。

选择 LangGraph 的原因是：

- 研究、数据更新、筛选、策略校验和回测有明确步骤。
- 需要保留研究上下文和中间结果。
- 证据不足、数据缺失和策略校验失败需要进入不同处理分支。

具体的 checkpoint、任务队列、异步回测和持久化方案暂不确定，等首期流程跑通后再决定。

### 首期 Workflow

```text
理解请求
  -> 判断请求类型
  -> 检查数据
  -> 必要时同步数据
  -> 执行研究、筛选或策略校验
  -> 生成回测任务（需要时）
  -> 检查证据是否充分
  -> 生成结果或证据缺口说明
```

首期只需要支持三类主要请求：

1. 研究和筛选红利 ETF。
2. 编写或修改交易策略。
3. 调用回测工具并解释回测结果。

模型可以负责理解用户意图、补充必要参数和组织表达；数据同步、指标计算、筛选、策略校验和回测必须由确定性代码或工具完成。Graph 的分支判断应基于结构化结果，不依赖模型自由发挥。

### 首期工具集合

| 工具 | 职责 |
| --- | --- |
| `sync_dividend_etf_data` | 同步和检查红利 ETF 数据 |
| `screen_dividend_etfs` | 按筛选条件计算并返回候选 ETF |
| `validate_strategy` | 检查策略格式、参数和基本无前视风险 |
| `run_backtest` | 执行日级别策略回测 |
| `get_backtest_result` | 查询和读取回测结果 |

工具返回必须包含结构化结果、数据来源、数据日期、计算口径、假设、警告和证据状态。具体字段模型后续实现工具时再确定。

## 8. Web 架构与开发顺序

### 架构边界

- Agent、领域服务和工具属于独立的 Python 核心层，不依赖 Web 框架或前端实现。
- 后端负责对外提供 API、会话管理、任务编排、鉴权及 Agent 调用入口，不在路由或控制器中实现指标计算、筛选和回测逻辑。
- Web 前端通过稳定、版本化的 API 与后端通信，不直接读取数据存储，也不直接调用 Agent 内部模块。
- Agent 与后端、后端与前端之间优先使用结构化请求和响应模型，明确错误、警告、证据状态和任务状态。
- 长时间运行的数据同步和回测应保留异步任务接口边界；具体队列和推送方案在后端阶段确定。

### 开发顺序

1. 当前阶段：优先完成 Agent、领域服务、确定性工具及其测试。
2. 后端阶段：在 Agent 接口稳定后，开发独立后端 API，并接入 Agent Workflow。
3. 前端阶段：在后端 API 契约明确后开发 Web UI，实现用户与 Agent 的交互及研究、筛选和回测结果展示。

在当前 Agent 开发阶段，不提前耦合具体后端或前端框架；但新增接口和数据模型时，应考虑后续通过 API 序列化、异步执行和 Web 展示的需要。

## 9. 建议目录

目录只作为初步边界，后续根据实际代码调整：

```text
src/dividendlab/
  data/       # 数据同步、清洗和校验
  screening/  # ETF 筛选和指标
  strategy/   # 策略接口和策略管理
  backtest/   # 回测引擎
  tools/      # Agent 可调用工具
  agent/      # LangGraph Workflow
tests/

# 后续阶段按需增加，名称可根据采用的框架调整
backend/      # 独立后端服务与 API
frontend/     # 独立 Web 前端
```

回测和指标计算不依赖自然语言层；工具和领域服务应能够单独测试。

## 10. 分阶段计划

### 第一阶段：数据与单只 ETF 研究

- 确定首个可用数据源。
- 完成红利 ETF 候选池和日级行情、分红数据同步。
- 对单只 ETF 计算分红收益率、行业权重、最大回撤和修复时间。
- 产出带来源的基础研究结果。

### 第二阶段：筛选工具

- 实现行业权重不均匀、股息、月月分红和回撤条件。
- 输出候选 ETF、指标、淘汰原因和证据。

### 第三阶段：策略与回测

- 定义 Python 策略接口。
- 完成最小可用的日级回测工具。
- 支持一个买入持有策略和一个定期调仓示例。

### 第四阶段：Agent Workflow

- 使用 LangGraph 串联数据、筛选、策略和回测工具。
- 增加证据检查和证据不足分支。
- 根据真实使用情况再增加异步任务、持久化和更复杂的恢复机制。

### 第五阶段：后端服务

- 定义并实现独立后端 API，封装 Agent 会话、研究、筛选和回测入口。
- 明确同步请求、异步任务、错误处理和证据数据的 API 契约。
- 保持后端与 Agent 核心层解耦，并为 API 和集成流程增加测试。

### 第六阶段：Web 前端

- 开发独立 Web UI，通过后端 API 与 Agent 交互。
- 展示对话、任务状态、研究证据、筛选结果和回测结果。
- 完善响应式布局、错误与空状态、长任务反馈和关键风险提示。

## 11. 初期验收标准

第一阶段完成时，应能够：

1. 获取并更新红利 ETF 的日级数据。
2. 查看单只 ETF 的分红、行业权重、最大回撤和修复时间。
3. 为每项结果提供来源、数据日期、计算口径和限制说明。

后续阶段再分别验收筛选、策略、回测和完整 Agent Workflow，不要求一次实现全部能力。

## 12. 暂不确定事项

以下内容在需要实现时再确定：

- 具体数据源、备用数据源及授权边界。
- 分红收益率、行业不均匀度和规模流动性的具体阈值。
- 默认基准和交易成本模型。
- 回测引擎和 Python 运行隔离方案。
- LangGraph 的持久化、异步任务和部署方式。
- 后端框架、API 协议、任务队列和实时消息方案。
- Web 前端框架、组件体系和部署方式。

## 13. 工程约定

- Python 为首选研究与回测语言。
- 使用 `uv` 统一管理 Python 版本、虚拟环境和项目依赖，以 `pyproject.toml` 声明依赖，并提交 `uv.lock` 保证可复现安装。
- 添加、删除和更新 Python 依赖时使用 `uv add`、`uv remove` 和 `uv lock`，运行项目命令优先使用 `uv run`；不并行维护 `requirements.txt` 等重复依赖清单，除非部署环境明确需要。
- 依赖按运行、开发和可选功能合理分组，新增依赖前应确认用途，避免 Agent 核心层引入后端或前端专用依赖。
- 前端与后端保持独立工程边界、依赖清单和构建流程，不将前端依赖混入 Python 项目依赖。
- 核心计算和数据处理必须有测试。
- 数据、策略、筛选和回测的结果应能根据相同输入复现。
- 新增规则或重要假设时，同步更新文档和测试。
- 不提交密钥、账号和不允许再分发的数据。
